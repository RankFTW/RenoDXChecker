<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RenoDXChecker.AboutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="About — RenoDX Mod Manager">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ScrollViewer.Background>
            <SolidColorBrush Color="#0C0C20"/>
        </ScrollViewer.Background>

        <StackPanel Padding="32,28,32,36" Spacing="16" MaxWidth="620">

            <!-- Hero -->
            <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,0,0,6">
                <Border Width="56" Height="56" CornerRadius="14" Background="#FF6B35">
                    <TextBlock Text="R" FontSize="30" FontWeight="Bold" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock FontSize="22" FontWeight="Bold">
                        <Run Text="RenoDX" Foreground="#FF6B35"/>
                        <Run Text=" Mod Manager" Foreground="#E0E0F0"/>
                    </TextBlock>
                    <TextBlock Text="Version 1.0.0  ·  by RankFTW" FontSize="12" Foreground="#505070" Margin="0,3,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Disclaimer -->
            <Border Background="#180A0A" CornerRadius="10" Padding="16,12"
                    BorderBrush="#3A1A1A" BorderThickness="1">
                <StackPanel Spacing="6">
                    <TextBlock Text="⚠  Disclaimer" FontSize="12" FontWeight="SemiBold" Foreground="#FAB387"/>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#8A7A6A" LineHeight="20">
                        <Run Text="This is an unofficial third-party tool. It is "/>
                        <Run Text="not affiliated with or endorsed by the RenoDX project." FontWeight="SemiBold"/>
                        <Run Text=" All mod files are fetched directly from official RenoDX GitHub snapshots."/>
                    </TextBlock>
                </StackPanel>
            </Border>

            <!-- What it does -->
            <Border Background="#0F0F28" CornerRadius="10" Padding="16,12" BorderBrush="#202040" BorderThickness="1">
                <StackPanel Spacing="8">
                    <TextBlock Text="What this app does" FontSize="12" FontWeight="SemiBold" Foreground="#CDD6F4"/>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#6A6A8A" LineHeight="20"
                               Text="Automatically detects games from Steam, GOG, Epic, and EA App. Matches them to the RenoDX wiki mod list. Installs and manages HDR mods with one click. Detects Unreal Engine and Unity games and offers generic engine plugins where no specific mod exists."/>
                </StackPanel>
            </Border>

            <!-- How to use -->
            <Border Background="#0F0F28" CornerRadius="10" Padding="16,12" BorderBrush="#202040" BorderThickness="1">
                <StackPanel Spacing="10">
                    <TextBlock Text="How to use" FontSize="12" FontWeight="SemiBold" Foreground="#CDD6F4"/>
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Background="#FF6B35" CornerRadius="12" Width="22" Height="22" MinWidth="22">
                                <TextBlock Text="1" FontSize="11" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#7A7A9A" LineHeight="19" VerticalAlignment="Center">
                                <Run FontWeight="SemiBold" Foreground="#CDD6F4">Install ReShade with Addon Support</Run>
                                <Run Text=" — download from reshade.me and select 'with Addon Support'. Install it into your game folder."/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Background="#FF6B35" CornerRadius="12" Width="22" Height="22" MinWidth="22">
                                <TextBlock Text="2" FontSize="11" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#7A7A9A" LineHeight="19" VerticalAlignment="Center">
                                <Run FontWeight="SemiBold" Foreground="#CDD6F4">Find your game</Run>
                                <Run Text=" — detected automatically. Use 📁 to set the folder manually if needed."/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Background="#FF6B35" CornerRadius="12" Width="22" Height="22" MinWidth="22">
                                <TextBlock Text="3" FontSize="11" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#7A7A9A" LineHeight="19" VerticalAlignment="Center">
                                <Run FontWeight="SemiBold" Foreground="#CDD6F4">Click Install</Run>
                                <Run Text=" — the .addon64 file is placed next to the game exe (Binaries\Win64 for Unreal Engine games)."/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Spacing="10">
                            <Border Background="#FF6B35" CornerRadius="12" Width="22" Height="22" MinWidth="22">
                                <TextBlock Text="4" FontSize="11" FontWeight="Bold" Foreground="White"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#7A7A9A" LineHeight="19" VerticalAlignment="Center">
                                <Run FontWeight="SemiBold" Foreground="#CDD6F4">Launch your game</Run>
                                <Run Text=" — press Home to open ReShade. Go to the Add-ons tab to configure RenoDX settings."/>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Links -->
            <Border Background="#0F0F28" CornerRadius="10" Padding="16,12" BorderBrush="#202040" BorderThickness="1">
                <StackPanel Spacing="8">
                    <TextBlock Text="Links" FontSize="12" FontWeight="SemiBold" Foreground="#CDD6F4"/>
                    <HyperlinkButton Content="🌐  RenoDX Project — github.com/clshortfuse/renodx"
                                     NavigateUri="https://github.com/clshortfuse/renodx"
                                     Foreground="#89B4FA" FontSize="12" Padding="0"/>
                    <HyperlinkButton Content="📋  RenoDX Mod List Wiki"
                                     NavigateUri="https://github.com/clshortfuse/renodx/wiki/Mods"
                                     Foreground="#89B4FA" FontSize="12" Padding="0"/>
                    <HyperlinkButton Content="🖥  ReShade (with Addon Support) — reshade.me"
                                     NavigateUri="https://reshade.me"
                                     Foreground="#89B4FA" FontSize="12" Padding="0"/>
                    <HyperlinkButton Content="👤  RankFTW on GitHub — github.com/RankFTW"
                                     NavigateUri="https://github.com/RankFTW"
                                     Foreground="#89B4FA" FontSize="12" Padding="0"/>
                </StackPanel>
            </Border>

            <!-- Credits -->
            <Border Background="#0F0F28" CornerRadius="10" Padding="16,12" BorderBrush="#202040" BorderThickness="1">
                <StackPanel Spacing="8">
                    <TextBlock Text="Credits" FontSize="12" FontWeight="SemiBold" Foreground="#CDD6F4"/>
                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#6A6A8A" LineHeight="20"
                               Text="All HDR mods are created and maintained by the RenoDX contributors. This app would not exist without their work."/>
                    <TextBlock FontSize="12" Foreground="#8A8AAA">
                        <Run Text="⭐  " Foreground="#FAB387"/>
                        <Run Text="clshortfuse" FontWeight="SemiBold" Foreground="#CDD6F4"/>
                        <Run Text=" — RenoDX creator &amp; primary maintainer" Foreground="#505070"/>
                    </TextBlock>
                    <TextBlock FontSize="12" Foreground="#8A8AAA">
                        <Run Text="⭐  " Foreground="#FAB387"/>
                        <Run Text="RenoDX community contributors" FontWeight="SemiBold" Foreground="#CDD6F4"/>
                        <Run Text=" — mod authors across 100+ games" Foreground="#505070"/>
                    </TextBlock>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Window>
